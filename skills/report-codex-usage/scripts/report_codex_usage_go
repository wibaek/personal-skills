#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cache_root="${TMPDIR:-/tmp}/report-codex-usage-go-cache"
export GOCACHE="$cache_root/build"
export REPORT_CODEX_USAGE_SKILL_ROOT="$script_dir/.."
cd "$script_dir/go"
exec go run . "$@"
